{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Proo\bject for storing Postman Te\b\btests",
  "directories": {
	"test": "tests"
  },
  "scripts": {
    "api-tests": "newman run tests/postman-newman-jenkins.postman_collection.json --environment tests/postman-newman-jenkins.postman_environment.json --reporters cli,junit --reporter-json-export newman/report.xml"
  },
  "author": "Pedro Marques",
  "dependencies": {
    "newman": "^3.10.0"
  }
}
